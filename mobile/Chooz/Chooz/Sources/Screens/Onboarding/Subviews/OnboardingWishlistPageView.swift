import SwiftUI

struct OnboardingPageView: View {
    
    // MARK: - Internal Properties
    
    struct Model {
        let image: Image
        let title: String
        let description: String
    }
    
    // MARK: - Body
    
    var body: some View {
        VStack(spacing: .zero) {
            
        }
    }
}
