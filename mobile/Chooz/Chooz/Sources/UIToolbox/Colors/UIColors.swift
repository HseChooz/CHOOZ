import UIKit

enum UIColors {
    
    enum Neutral {
        static let grey400 = UIColor(named: "grey400", in: .module, compatibleWith: nil)
    }
    
    enum Blue {
        static let blue500 = UIColor(named: "blue500", in: .module, compatibleWith: nil)
    }
    
}
